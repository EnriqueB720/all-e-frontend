query refreshUser($data: String!){
  refreshUser(data: $data){
    access_token,
    expiresAt,
    user{
      id
      email
      username
      language
      createdAt
      watch{
        id,
        serialNum
        metadataURI
        certificateUrl
        ownerId
        lastSynced
      },
    }
  }
}
