query getWatch($where: WatchWhereInput!) {
  watch(where: $where) {
    id
    serialNum
    metadataURI
    certificateUrl
    ownerId
    lastSynced
    user {
      id
      username
      email
      walletAddress
      createdAt
      language
    }
    ownershipLog {
      id
      watchId
      ownerId
      timestamp
      owner {
        id
        username
        walletAddress
      }
    }
  }
}
