query getWatch($where: WatchWhereInput!) {
  watch(where: $where) {
    id
    serialNum
    metadataURI
    certificateUrl
    ownerId
    lastSynced
    user {
      id
      username
      email
      walletAddress
    }
    ownershipLog {
      id
      ownerId
      timestamp
    }
  }
}
