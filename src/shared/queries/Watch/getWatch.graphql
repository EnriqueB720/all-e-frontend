query getWatch($where: WatchWhereInput!) {
  watch(where: $where) {
    id
    serialNum
    metadataURI
    ownerId
    lastSynced
    user {
      id
      username
      email
      walletAddress
    }
    ownershipLog {
      id
      ownerId
      timestamp
    }
  }
}
