query getUser($where: UserWhereInput!) {
  user(where: $where) {
    id
    email
    username
    createdAt
    language
    watch {
      id
      serialNum
      metadataURI
      ownerId
      lastSynced
    }
  }
}
